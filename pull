#!/bin/bash

set -e

MAIN="${MAIN:-main}"

source "$(dirname "$0")/rebase-or-abort"

if (git remote | grep --fixed-strings --line-regexp --quiet 'upstream') then
  git fetch upstream
  rebase_or_abort "upstream/$MAIN"
else
  git fetch origin
  rebase_or_abort "origin/$MAIN"
fi
